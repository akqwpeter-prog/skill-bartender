# Contributing

Small is the point. PRs that add rules or docs must keep the skill
under 120 lines of instructions and the policy table scannable.

Run `scripts/validate.py` before pushing; a change under `skills/`
also runs SkillSpector in CI.
